#!/usr/bin/env python3
"""
《Mind》— 一个活的、自更新权重的神经网络系统
============================================
核心思想：没有训练/推理之分。系统通电就在"活着"。
         输入像呼吸一样进来，输出像心跳一样流出去。
         权重每一秒都在变，结构从经历中自己长出来。

架构层：
  core/        — 核心认知网络（CTRNN + 权重更新 + 结构生长）
  memory/      — 联想记忆 + 书籍存储（轨道A/B）
  io/          — 多模态输入输出 + 跨模态预测
  interface/   — Web 接口（Flask 轻量前端）
  tests/       — 每模块的独立测试
  data/        — 持久化的权重和记忆文件

启动命令：
  python3 run.py

依赖：
  pip install flask numpy

作者：小云同学 + 老板（亦知）
版本：v0.1 "活着"
"""
